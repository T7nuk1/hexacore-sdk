```javascript
webviewView.webview.onDidReceiveMessage(message => {
            if (message.command === 'open') {
                const files = this.getTutorialFiles();
		                vscode.commands.executeCommand('sdkTutorials.openLesson', message.path, files);
            } else if (message.command === 'search') {
                this.handleSearch(message.text);
            }
        });
```

бау